{% extends 'base.html.twig' %}

{% form_theme form 'logo/imageUploadPreviews.html.twig' %}

{% block body %}
    <h1>{{ 'logo.create.heading' | trans }}</h1>

    <div class="col-md-8">
        {{ form_start(form, {'method': 'post'}) }}
        {{ form_widget(form) }}
        {{ form_end(form) }}
    </div>

{% endblock %}

{% block javascripts %}
    <script src="{{ asset('js/imagePreview.js') }}"></script>
{% endblock %}